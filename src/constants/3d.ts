import { SectionTitle } from '@/types/enums/SectionTitle';

/**
 * Title of the sections where the perspective camera should be fixed
 */
export const CAMERA_FIXED_SECTION_TITLES = [
  SectionTitle.Projects,
  SectionTitle.ContactMe,
];
