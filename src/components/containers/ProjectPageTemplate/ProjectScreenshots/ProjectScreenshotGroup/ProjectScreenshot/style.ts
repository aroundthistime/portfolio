import styled from 'styled-components';

export const ProjectScreenshotContainer = styled.li`
  .project-screenshot {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }
`;
